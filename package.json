{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },

  "homepage": "https://deraingo.github.io/reactResumeSite/",
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
